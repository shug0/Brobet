import styled from 'styled-components'
import { key } from 'styled-theme'

const Wrapper = styled.section`
  display: flex;
  margin: auto;
  max-width: ${key('comps.wrapper.w')};
  width: 100%;
  padding: 2rem;
`

export default Wrapper
